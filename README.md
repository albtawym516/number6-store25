# NUMBER 6 - متجر إلكتروني احترافي متكامل

هذا المشروع هو تطبيق ويب لمتجر إلكتروني باسم "NUMBER 6"، متخصص في بيع ملابس الرجال والنساء والأطفال. تم بناء المشروع باستخدام React للواجهة الأمامية و Node.js/Express للخادم، وهو متصل بقاعدة بيانات MongoDB حقيقية، ويدعم الدفع الإلكتروني عبر Stripe، ويرسل إشعارات بالطلبات الجديدة عبر البريد الإلكتروني.

## ✨ الميزات الرئيسية

- **قاعدة بيانات حقيقية:** يتم حفظ جميع المنتجات والطلبات بشكل دائم في قاعدة بيانات MongoDB Atlas.
- **بوابة دفع إلكتروني:** دمج آمن لبوابة Stripe لقبول المدفوعات بالبطاقات الائتمانية.
- **نظام إشعارات فوري:** يتم إرسال بريد إلكتروني تلقائيًا إلى مدير المتجر عند استلام أي طلب جديد.
- **واجهة تحكم قوية:** إدارة كاملة للمنتجات، الطلبات، وتخصيص مظهر المتجر.
- **أمان:** لوحة تحكم محمية بكلمة مرور تتصل بالخادم للتحقق.
- **تجربة تطوير حديثة:** استخدام Vite, TypeScript, و Nodemon لتطوير سريع وفعال.

## 🛠️ التقنيات المستخدمة

- **الواجهة الأمامية (Frontend):** React, TypeScript, Tailwind CSS, React Router, Stripe.js
- **الخادم (Backend):** Node.js, Express, Mongoose (MongoDB), Stripe API, Nodemailer
- **قاعدة البيانات:** MongoDB Atlas
- **أدوات التطوير (Dev Tools):** Vite, ESLint, Nodemon, Concurrently

## 🚀 كيفية تشغيل المشروع

لتشغيل هذا المشروع محليًا، اتبع الخطوات التالية:

### المتطلبات المسبقة
- Node.js (v16 أو أحدث)
- npm (عادةً ما يأتي مع Node.js)
- حساب MongoDB Atlas (الخطة المجانية كافية)
- حساب Stripe (للحصول على مفاتيح الاختبار)
- حساب Gmail (لإنشاء كلمة مرور للتطبيقات لإرسال الإيميلات)

### 1. استنساخ المشروع وتثبيت الاعتماديات
```bash
git clone <repository_url>
cd <project_folder>
npm install
```

### 2. إعداد متغيرات البيئة
- انسخ ملف `.env.example` إلى ملف جديد باسم `.env`:
```bash
cp .env.example .env
```
- املأ الفراغات في ملف `.env` بالبيانات والمفاتيح الخاصة بك:
  - **MONGO_URI**: رابط الاتصال بقاعدة بيانات MongoDB Atlas
  - **STRIPE_SECRET_KEY**: المفتاح السري لـ Stripe (يبدأ بـ sk_test_)
  - **STRIPE_PUBLISHABLE_KEY**: المفتاح العام لـ Stripe (يبدأ بـ pk_test_)
  - **EMAIL_HOST**: smtp.gmail.com (للبريد Gmail)
  - **EMAIL_PORT**: 587
  - **EMAIL_USER**: بريدك الإلكتروني لإرسال الإشعارات
  - **EMAIL_PASS**: كلمة مرور التطبيقات (ليست كلمة مرور Gmail العادية)
  - **ADMIN_EMAIL**: البريد الإلكتروني لاستلام إشعارات الطلبات
  - **ADMIN_PASSWORD**: كلمة مرور لوحة التحكم (اختياري، الافتراضي: 01206155419)

### 3. تشغيل بيئة التطوير
استخدم الأمر التالي لتشغيل الواجهة الأمامية والخادم معًا:
```bash
npm run dev
```
- ستعمل الواجهة الأمامية على `http://localhost:5173`.
- سيعمل الخادم على `http://localhost:3001`.

### 4. الوصول للتطبيق
- **المتجر:** افتح المتصفح على `http://localhost:5173`.
- **لوحة التحكم:** اذهب إلى `http://localhost:5173/admin`.
  - **كلمة المرور:** `01206155419` (أو كلمة المرور التي حددتها في متغيرات البيئة)

## 🔒 ملاحظات الأمان

- **كلمة مرور الأدمن:** يُنصح بشدة بتغيير كلمة المرور الافتراضية عبر متغير البيئة `ADMIN_PASSWORD`
- **متغيرات البيئة:** يتم تجاهل ملفات `.env` تلقائياً عبر Git، لا تشارك هذه الملفات أبداً
- **بيئة الإنتاج:** استخدم مفاتيح حقيقية لـ Stripe وإعدادات بريد إلكتروني موثوقة
- **HTTPS:** في بيئة الإنتاج، تأكد من استخدام HTTPS لتأمين جميع الاتصالات

## ✨ الميزات المضافة والمحسّنة

### 🆕 ميزات جديدة
- **صفحة 404 مخصصة:** تصميم عصري لصفحة الخطأ مع روابط سريعة
- **التحقق من صحة المدخلات:** حماية قوية ضد البيانات غير الصالحة
- **دعم PWA:** إمكانية تثبيت التطبيق على الأجهزة المحمولة
- **أنماط CSS مخصصة:** تحسينات بصرية وتجربة مستخدم أفضل

### 🔧 تحسينات فنية
- **إصلاح مشاكل الخادم:** توحيد ملفات الخادم وإزالة الملفات المكررة
- **تحسين HTML:** إصلاح الأخطاء وإزالة الأكواد غير الضرورية
- **تحديث .gitignore:** حماية أفضل للملفات الحساسة
- **هيكلة أفضل:** تنظيم الملفات والمجلدات بشكل احترافي

### 🛡️ تحسينات الأمان
- **التحقق من صحة البيانات:** في جميع نقاط النهاية API
- **تأمين كلمة المرور:** استخدام متغيرات البيئة بدلاً من الأكواد الثابتة
- **حماية المدخلات:** منع هجمات XSS و SQL Injection
